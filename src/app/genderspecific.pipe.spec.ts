import { GenderspecificPipe } from './genderspecific.pipe';

describe('GenderspecificPipe', () => {
  it('create an instance', () => {
    const pipe = new GenderspecificPipe();
    expect(pipe).toBeTruthy();
  });
});
